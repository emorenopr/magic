# Copia este archivo a .env (uso local) y pon tu llave real.
# En Vercel, agrega ANTHROPIC_API_KEY como variable de entorno del proyecto
# (Project Settings -> Environment Variables) en vez de subir un .env.
ANTHROPIC_API_KEY=
